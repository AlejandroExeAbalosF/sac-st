# Credenciales del rol dueño del esquema. Copiar a /etc/sacst/migrate.env.
#
# Solo las usa el servicio `migrate`, que carga app.env y encima esto: el
# resto del stack nunca las ve. Ver deploy/postgres/setup.sql.
DB_USERNAME=sacst_owner
DB_PASSWORD=
